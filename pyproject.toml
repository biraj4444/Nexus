[project]
name = "nexusvault"
version = "0.1.0"
description = "NexusVault collection platform"
requires-python = ">=3.13"
dependencies = [
    "flask",
    "werkzeug",
    "pymongo",
    "cloudinary",
    "python-dotenv",
    "requests",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
